# Module 4.4: Containerization & Cloud Deployment - Workflow Cheatsheets

## Workflow 1: Docker Build → Local Test → Deploy

### When to Use
Standard workflow for any containerized ML deployment.

### Step-by-Step

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Create Dockerfile                                   │
├─────────────────────────────────────────────────────────────┤
│ □ Start from NGC base image                                 │
│ □ Add dependencies                                          │
│ □ Copy application code                                     │
│ □ Set entrypoint                                            │
│                                                             │
│ Code:                                                       │
│ ```dockerfile                                               │
│ FROM nvcr.io/nvidia/pytorch:25.11-py3                       │
│ WORKDIR /app                                                │
│ COPY requirements.txt .                                     │
│ RUN pip install -r requirements.txt                         │
│ COPY . /app                                                 │
│ EXPOSE 8000                                                 │
│ CMD ["python", "serve.py"]                                  │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Dockerfile created                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: Build Image                                         │
├─────────────────────────────────────────────────────────────┤
│ □ Build with appropriate tag                                │
│ □ Check image size                                          │
│                                                             │
│ Code:                                                       │
│ ```bash                                                     │
│ docker build -t myapp:v1 .                                  │
│ docker images myapp                                         │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Image built, reasonable size                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 3: Test Locally                                        │
├─────────────────────────────────────────────────────────────┤
│ □ Run container with GPU                                    │
│ □ Test health endpoint                                      │
│ □ Test inference                                            │
│                                                             │
│ Code:                                                       │
│ ```bash                                                     │
│ docker run --gpus all -p 8000:8000 myapp:v1                 │
│ curl http://localhost:8000/health                           │
│ curl -X POST http://localhost:8000/predict -d '...'         │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: All endpoints respond correctly               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 4: Push to Registry                                    │
├─────────────────────────────────────────────────────────────┤
│ □ Tag for registry                                          │
│ □ Push image                                                │
│                                                             │
│ Code:                                                       │
│ ```bash                                                     │
│ docker tag myapp:v1 registry.example.com/myapp:v1           │
│ docker push registry.example.com/myapp:v1                   │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Image accessible from registry                │
└─────────────────────────────────────────────────────────────┘
```

---

## Workflow 2: AWS SageMaker Deployment

### When to Use
Deploying ML models to AWS with managed infrastructure.

### Step-by-Step

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Package Model                                       │
├─────────────────────────────────────────────────────────────┤
│ □ Create model.tar.gz with model artifacts                  │
│ □ Upload to S3                                              │
│                                                             │
│ Code:                                                       │
│ ```bash                                                     │
│ tar -czvf model.tar.gz model/ code/                         │
│ aws s3 cp model.tar.gz s3://bucket/models/                  │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Model in S3                                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: Create SageMaker Model                              │
├─────────────────────────────────────────────────────────────┤
│ □ Define model with container and S3 path                   │
│                                                             │
│ Code:                                                       │
│ ```python                                                   │
│ from sagemaker.huggingface import HuggingFaceModel          │
│                                                             │
│ model = HuggingFaceModel(                                   │
│     model_data="s3://bucket/models/model.tar.gz",           │
│     role=role,                                              │
│     transformers_version="4.41",                            │
│     pytorch_version="2.3",                                  │
│     py_version="py311"                                      │
│ )                                                           │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Model object created                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 3: Deploy Endpoint                                     │
├─────────────────────────────────────────────────────────────┤
│ □ Choose instance type                                      │
│ □ Deploy                                                    │
│ □ Wait for InService status                                 │
│                                                             │
│ Code:                                                       │
│ ```python                                                   │
│ predictor = model.deploy(                                   │
│     initial_instance_count=1,                               │
│     instance_type="ml.g5.xlarge",                           │
│     endpoint_name="my-endpoint"                             │
│ )                                                           │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Endpoint status = InService                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 4: Test and Monitor                                    │
├─────────────────────────────────────────────────────────────┤
│ □ Test inference                                            │
│ □ Check CloudWatch metrics                                  │
│ □ Configure auto-scaling                                    │
│                                                             │
│ Code:                                                       │
│ ```python                                                   │
│ response = predictor.predict({"inputs": "Hello!"})          │
│ print(response)                                             │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Predictions working, metrics visible          │
└─────────────────────────────────────────────────────────────┘
```

---

## Workflow 3: Kubernetes Deployment

### When to Use
Production deployments requiring orchestration, scaling, and full control.

### Step-by-Step

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Create Manifests                                    │
├─────────────────────────────────────────────────────────────┤
│ □ Deployment.yaml                                           │
│ □ Service.yaml                                              │
│ □ HPA.yaml (optional)                                       │
│                                                             │
│ Code: (deployment.yaml)                                     │
│ ```yaml                                                     │
│ apiVersion: apps/v1                                         │
│ kind: Deployment                                            │
│ metadata:                                                   │
│   name: inference                                           │
│ spec:                                                       │
│   replicas: 2                                               │
│   template:                                                 │
│     spec:                                                   │
│       containers:                                           │
│       - name: inference                                     │
│         image: myapp:v1                                     │
│         resources:                                          │
│           limits:                                           │
│             nvidia.com/gpu: 1                               │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Valid YAML files                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: Deploy to Cluster                                   │
├─────────────────────────────────────────────────────────────┤
│ □ Apply manifests                                           │
│ □ Wait for pods ready                                       │
│                                                             │
│ Code:                                                       │
│ ```bash                                                     │
│ kubectl apply -f deployment.yaml                            │
│ kubectl apply -f service.yaml                               │
│ kubectl get pods -w  # Watch until Running                  │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Pods in Running state                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 3: Expose and Test                                     │
├─────────────────────────────────────────────────────────────┤
│ □ Get service endpoint                                      │
│ □ Test inference                                            │
│                                                             │
│ Code:                                                       │
│ ```bash                                                     │
│ kubectl get svc                                             │
│ curl http://<EXTERNAL-IP>/predict                           │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: External access working                       │
└─────────────────────────────────────────────────────────────┘
```

---

## Workflow 4: Gradio Demo → HF Spaces

### When to Use
Quickly deploying interactive ML demos for public access.

### Step-by-Step

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Create Gradio App                                   │
├─────────────────────────────────────────────────────────────┤
│ □ Create app.py with interface                              │
│ □ Test locally                                              │
│                                                             │
│ Code:                                                       │
│ ```python                                                   │
│ import gradio as gr                                         │
│                                                             │
│ def predict(text):                                          │
│     return f"Processed: {text}"                             │
│                                                             │
│ demo = gr.Interface(fn=predict, inputs="text", outputs="text")│
│ demo.launch()                                               │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Works at localhost:7860                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: Create HF Space                                     │
├─────────────────────────────────────────────────────────────┤
│ □ Go to huggingface.co/spaces                               │
│ □ Create new Space                                          │
│ □ Select Gradio SDK                                         │
│                                                             │
│ ✓ Checkpoint: Empty Space created                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 3: Upload Files                                        │
├─────────────────────────────────────────────────────────────┤
│ □ Upload app.py                                             │
│ □ Upload requirements.txt                                   │
│ □ Add README.md with metadata                               │
│                                                             │
│ README.md:                                                  │
│ ```yaml                                                     │
│ ---                                                         │
│ title: My Demo                                              │
│ emoji: 🚀                                                   │
│ sdk: gradio                                                 │
│ sdk_version: 4.0.0                                          │
│ ---                                                         │
│ ```                                                         │
│                                                             │
│ ✓ Checkpoint: Space building                                │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 4: Share                                               │
├─────────────────────────────────────────────────────────────┤
│ □ Wait for build to complete                                │
│ □ Test public URL                                           │
│ □ Share link                                                │
│                                                             │
│ URL: https://huggingface.co/spaces/username/my-demo         │
│                                                             │
│ ✓ Checkpoint: Public demo accessible                        │
└─────────────────────────────────────────────────────────────┘
```

---

## Deployment Decision Flowchart

```
                    Start: Deploy Model
                           │
                           ▼
              ┌────────────────────────┐
              │ Who is the audience?   │
              └───────────┬────────────┘
                          │
          ┌───────────────┼───────────────┐
          │               │               │
     Internal         Demo/POC        Production
          │               │               │
          ▼               ▼               ▼
  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
  │ Docker       │ │ HF Spaces or │ │ Cloud or K8s │
  │ Compose      │ │ Streamlit    │ │              │
  └──────────────┘ └──────────────┘ └──────┬───────┘
                                           │
                              ┌────────────┴────────────┐
                              │ Have DevOps team?       │
                              └────────────┬────────────┘
                                           │
                              ┌────────────┴────────────┐
                              │ No                  Yes │
                              ▼                        ▼
                      [SageMaker/Vertex]        [Kubernetes]
```
